{
  "name": "question-harvester",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --test tests/app.test.mjs",
    "sample": "node scripts/create-sample.mjs"
  }
}
